import 'package:flutter/material.dart';

class MealStatusPage extends StatelessWidget {
  const MealStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('식사 신청현황', style: TextStyle(fontSize: 20)));
  }
}
