import 'package:flutter/material.dart';

class MealApplyPage extends StatelessWidget {
  const MealApplyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('식사 신청하기', style: TextStyle(fontSize: 20)));
  }
}
