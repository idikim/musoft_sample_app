
import 'package:flutter/material.dart';

class CounselLogPage extends StatelessWidget {
  const CounselLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('상담일지 페이지'),
    );
  }
}
