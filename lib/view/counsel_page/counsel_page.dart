import 'package:flutter/material.dart';

class CounselPage extends StatelessWidget {
  const CounselPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('상담 화면', style: TextStyle(fontSize: 24)));
  }
}
